# autofix

Claude Code skills for auditing and remediating design/quality findings.

## Skills

- **audit** — dispatcher that runs quality validators and emits findings in the shared contract.
- **autofix** — platform-agnostic remediation engine that applies fixes from an audit.

Skills live under `.claude/skills/`. Sourced from the `skill-1` branch of `claude-design-eds`.
